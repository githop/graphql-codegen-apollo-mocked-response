{
  "name": "graphql-codegen-apollo-mocked-response-root",
  "version": "0.0.0",
  "description": "",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "pnpm test -r",
    "generate": "pnpm --filter 'example' generate",
    "build": "pnpm --filter 'graphql-codegen-apollo-mocked-response' build && pnpm generate"
  },
  "type": "module",
  "keywords": [],
  "author": "githop",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.0.3",
    "@vitejs/plugin-react": "^1.3.2",
    "@vitest/ui": "^0.19.1",
    "prettier": "^2.7.1",
    "typescript": "^4.7.4",
    "vitest": "^0.17.0"
  }
}
